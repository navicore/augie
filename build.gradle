// Top-level build file where you can add configuration options common to all sub-projects/modules.

// hack since studio keeps adding android-facet to root holder proj
task assemble{}

task wrapper(type: Wrapper) {
    gradleVersion = '1.7'
}

